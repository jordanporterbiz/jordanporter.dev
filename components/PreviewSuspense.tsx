"use client";

// Once rollup supports "use client" module directives, 
// we can remove this as re-export will no longer be necessary.
export { PreviewSuspense as default } from "next-sanity/preview";